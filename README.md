# Movie-App-API- Base

Simple Express API for a movie discovery app.

## Routes
GET /           → API status  
GET /movies     → all movies  
GET /search?q=  → search by title  
GET /movies/genre?genre=Sci-Fi → filter by genre

## Run locally
npm install  
npm start
